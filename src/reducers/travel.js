import { travel } from "./initialState";
import {} from "../actions/actionTypes";

export default function uiReducer(state = travel, action) {
  switch (action.type) {
    default:
      return state;
  }
}
